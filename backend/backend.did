service : {
  finalizeTranscription: (nat) -> (bool);
  generateStudyGuide: (text) -> (text);
  getLatestTranscription: (nat) -> (opt text) query;
  processTranscription: (nat, text) -> (bool);
  startTranscription: () -> (nat);
}
